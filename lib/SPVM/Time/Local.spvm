class Time::Local {
  use Time::Info;
  use Time;
  
  static method timelocal : long ($time_info : Time::Info) {
    return Time->timelocal($time_info);
  }
  static method timegm : long ($time_info : Time::Info) {
    return Time->timegm($time_info);
  }
}

